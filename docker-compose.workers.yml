version: '3.8'

services:

  # Worker 1 - Local Postgres
  postgres-local-1:
    image: postgres:15-alpine
    container_name: postgres-local-1
    environment:
      POSTGRES_DB: fuzzilli_local
      POSTGRES_USER: fuzzilli
      POSTGRES_PASSWORD: fuzzilli123
      POSTGRES_INITDB_ARGS: "--encoding=UTF-8 --lc-collate=C --lc-ctype=C"
    volumes:
      - postgres_local_data_1:/var/lib/postgresql/data
      - /home/tropic/vrig/fuzzilli-vrig-proj/fuzzillai/postgres-init.sql:/docker-entrypoint-initdb.d/init.sql
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U fuzzilli -d fuzzilli_local"]
      interval: 10s
      timeout: 5s
      retries: 5
    restart: unless-stopped
    networks:
      - fuzzing-network

  # Worker 1 - Fuzzilli Container
  fuzzer-worker-1:
    build:
      context: /home/tropic/vrig/fuzzilli-vrig-proj/fuzzillai
      dockerfile: Cloud/VRIG/Dockerfile.distributed
    container_name: fuzzer-worker-1
    environment:
      - MASTER_POSTGRES_URL=postgresql://fuzzilli:fuzzilli123@postgres-master:5432/fuzzilli_master
      - LOCAL_POSTGRES_URL=postgresql://fuzzilli:fuzzilli123@postgres-local-1:5432/fuzzilli_local
      - POSTGRES_URL=postgresql://fuzzilli:fuzzilli123@postgres-local-1:5432/fuzzilli_local
      - FUZZER_INSTANCE_NAME=fuzzer-1
      - SYNC_INTERVAL=300
      - TIMEOUT=2500
      - MIN_MUTATIONS_PER_SAMPLE=25
      - DEBUG_LOGGING=false
    depends_on:
      postgres-master:
        condition: service_healthy
      postgres-local-1:
        condition: service_healthy
    volumes:
      - fuzzer_data_1:/home/app/Corpus
      - /home/tropic/vrig/fuzzilli-vrig-proj/fuzzbuild:/home/app/fuzzbuild:ro
    restart: unless-stopped
    healthcheck:
      test: ["CMD-SHELL", "pgrep -f FuzzilliCli || exit 1"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 60s
    networks:
      - fuzzing-network
    deploy:
      resources:
        limits:
          memory: 2G
        reservations:
          memory: 1G


  # Worker 2 - Local Postgres
  postgres-local-2:
    image: postgres:15-alpine
    container_name: postgres-local-2
    environment:
      POSTGRES_DB: fuzzilli_local
      POSTGRES_USER: fuzzilli
      POSTGRES_PASSWORD: fuzzilli123
      POSTGRES_INITDB_ARGS: "--encoding=UTF-8 --lc-collate=C --lc-ctype=C"
    volumes:
      - postgres_local_data_2:/var/lib/postgresql/data
      - /home/tropic/vrig/fuzzilli-vrig-proj/fuzzillai/postgres-init.sql:/docker-entrypoint-initdb.d/init.sql
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U fuzzilli -d fuzzilli_local"]
      interval: 10s
      timeout: 5s
      retries: 5
    restart: unless-stopped
    networks:
      - fuzzing-network

  # Worker 2 - Fuzzilli Container
  fuzzer-worker-2:
    build:
      context: /home/tropic/vrig/fuzzilli-vrig-proj/fuzzillai
      dockerfile: Cloud/VRIG/Dockerfile.distributed
    container_name: fuzzer-worker-2
    environment:
      - MASTER_POSTGRES_URL=postgresql://fuzzilli:fuzzilli123@postgres-master:5432/fuzzilli_master
      - LOCAL_POSTGRES_URL=postgresql://fuzzilli:fuzzilli123@postgres-local-2:5432/fuzzilli_local
      - POSTGRES_URL=postgresql://fuzzilli:fuzzilli123@postgres-local-2:5432/fuzzilli_local
      - FUZZER_INSTANCE_NAME=fuzzer-2
      - SYNC_INTERVAL=300
      - TIMEOUT=2500
      - MIN_MUTATIONS_PER_SAMPLE=25
      - DEBUG_LOGGING=false
    depends_on:
      postgres-master:
        condition: service_healthy
      postgres-local-2:
        condition: service_healthy
    volumes:
      - fuzzer_data_2:/home/app/Corpus
      - /home/tropic/vrig/fuzzilli-vrig-proj/fuzzbuild:/home/app/fuzzbuild:ro
    restart: unless-stopped
    healthcheck:
      test: ["CMD-SHELL", "pgrep -f FuzzilliCli || exit 1"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 60s
    networks:
      - fuzzing-network
    deploy:
      resources:
        limits:
          memory: 2G
        reservations:
          memory: 1G


  # Worker 3 - Local Postgres
  postgres-local-3:
    image: postgres:15-alpine
    container_name: postgres-local-3
    environment:
      POSTGRES_DB: fuzzilli_local
      POSTGRES_USER: fuzzilli
      POSTGRES_PASSWORD: fuzzilli123
      POSTGRES_INITDB_ARGS: "--encoding=UTF-8 --lc-collate=C --lc-ctype=C"
    volumes:
      - postgres_local_data_3:/var/lib/postgresql/data
      - /home/tropic/vrig/fuzzilli-vrig-proj/fuzzillai/postgres-init.sql:/docker-entrypoint-initdb.d/init.sql
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U fuzzilli -d fuzzilli_local"]
      interval: 10s
      timeout: 5s
      retries: 5
    restart: unless-stopped
    networks:
      - fuzzing-network

  # Worker 3 - Fuzzilli Container
  fuzzer-worker-3:
    build:
      context: /home/tropic/vrig/fuzzilli-vrig-proj/fuzzillai
      dockerfile: Cloud/VRIG/Dockerfile.distributed
    container_name: fuzzer-worker-3
    environment:
      - MASTER_POSTGRES_URL=postgresql://fuzzilli:fuzzilli123@postgres-master:5432/fuzzilli_master
      - LOCAL_POSTGRES_URL=postgresql://fuzzilli:fuzzilli123@postgres-local-3:5432/fuzzilli_local
      - POSTGRES_URL=postgresql://fuzzilli:fuzzilli123@postgres-local-3:5432/fuzzilli_local
      - FUZZER_INSTANCE_NAME=fuzzer-3
      - SYNC_INTERVAL=300
      - TIMEOUT=2500
      - MIN_MUTATIONS_PER_SAMPLE=25
      - DEBUG_LOGGING=false
    depends_on:
      postgres-master:
        condition: service_healthy
      postgres-local-3:
        condition: service_healthy
    volumes:
      - fuzzer_data_3:/home/app/Corpus
      - /home/tropic/vrig/fuzzilli-vrig-proj/fuzzbuild:/home/app/fuzzbuild:ro
    restart: unless-stopped
    healthcheck:
      test: ["CMD-SHELL", "pgrep -f FuzzilliCli || exit 1"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 60s
    networks:
      - fuzzing-network
    deploy:
      resources:
        limits:
          memory: 2G
        reservations:
          memory: 1G


  # Worker 4 - Local Postgres
  postgres-local-4:
    image: postgres:15-alpine
    container_name: postgres-local-4
    environment:
      POSTGRES_DB: fuzzilli_local
      POSTGRES_USER: fuzzilli
      POSTGRES_PASSWORD: fuzzilli123
      POSTGRES_INITDB_ARGS: "--encoding=UTF-8 --lc-collate=C --lc-ctype=C"
    volumes:
      - postgres_local_data_4:/var/lib/postgresql/data
      - /home/tropic/vrig/fuzzilli-vrig-proj/fuzzillai/postgres-init.sql:/docker-entrypoint-initdb.d/init.sql
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U fuzzilli -d fuzzilli_local"]
      interval: 10s
      timeout: 5s
      retries: 5
    restart: unless-stopped
    networks:
      - fuzzing-network

  # Worker 4 - Fuzzilli Container
  fuzzer-worker-4:
    build:
      context: /home/tropic/vrig/fuzzilli-vrig-proj/fuzzillai
      dockerfile: Cloud/VRIG/Dockerfile.distributed
    container_name: fuzzer-worker-4
    environment:
      - MASTER_POSTGRES_URL=postgresql://fuzzilli:fuzzilli123@postgres-master:5432/fuzzilli_master
      - LOCAL_POSTGRES_URL=postgresql://fuzzilli:fuzzilli123@postgres-local-4:5432/fuzzilli_local
      - POSTGRES_URL=postgresql://fuzzilli:fuzzilli123@postgres-local-4:5432/fuzzilli_local
      - FUZZER_INSTANCE_NAME=fuzzer-4
      - SYNC_INTERVAL=300
      - TIMEOUT=2500
      - MIN_MUTATIONS_PER_SAMPLE=25
      - DEBUG_LOGGING=false
    depends_on:
      postgres-master:
        condition: service_healthy
      postgres-local-4:
        condition: service_healthy
    volumes:
      - fuzzer_data_4:/home/app/Corpus
      - /home/tropic/vrig/fuzzilli-vrig-proj/fuzzbuild:/home/app/fuzzbuild:ro
    restart: unless-stopped
    healthcheck:
      test: ["CMD-SHELL", "pgrep -f FuzzilliCli || exit 1"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 60s
    networks:
      - fuzzing-network
    deploy:
      resources:
        limits:
          memory: 2G
        reservations:
          memory: 1G


  # Worker 5 - Local Postgres
  postgres-local-5:
    image: postgres:15-alpine
    container_name: postgres-local-5
    environment:
      POSTGRES_DB: fuzzilli_local
      POSTGRES_USER: fuzzilli
      POSTGRES_PASSWORD: fuzzilli123
      POSTGRES_INITDB_ARGS: "--encoding=UTF-8 --lc-collate=C --lc-ctype=C"
    volumes:
      - postgres_local_data_5:/var/lib/postgresql/data
      - /home/tropic/vrig/fuzzilli-vrig-proj/fuzzillai/postgres-init.sql:/docker-entrypoint-initdb.d/init.sql
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U fuzzilli -d fuzzilli_local"]
      interval: 10s
      timeout: 5s
      retries: 5
    restart: unless-stopped
    networks:
      - fuzzing-network

  # Worker 5 - Fuzzilli Container
  fuzzer-worker-5:
    build:
      context: /home/tropic/vrig/fuzzilli-vrig-proj/fuzzillai
      dockerfile: Cloud/VRIG/Dockerfile.distributed
    container_name: fuzzer-worker-5
    environment:
      - MASTER_POSTGRES_URL=postgresql://fuzzilli:fuzzilli123@postgres-master:5432/fuzzilli_master
      - LOCAL_POSTGRES_URL=postgresql://fuzzilli:fuzzilli123@postgres-local-5:5432/fuzzilli_local
      - POSTGRES_URL=postgresql://fuzzilli:fuzzilli123@postgres-local-5:5432/fuzzilli_local
      - FUZZER_INSTANCE_NAME=fuzzer-5
      - SYNC_INTERVAL=300
      - TIMEOUT=2500
      - MIN_MUTATIONS_PER_SAMPLE=25
      - DEBUG_LOGGING=false
    depends_on:
      postgres-master:
        condition: service_healthy
      postgres-local-5:
        condition: service_healthy
    volumes:
      - fuzzer_data_5:/home/app/Corpus
      - /home/tropic/vrig/fuzzilli-vrig-proj/fuzzbuild:/home/app/fuzzbuild:ro
    restart: unless-stopped
    healthcheck:
      test: ["CMD-SHELL", "pgrep -f FuzzilliCli || exit 1"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 60s
    networks:
      - fuzzing-network
    deploy:
      resources:
        limits:
          memory: 2G
        reservations:
          memory: 1G


  # Worker 6 - Local Postgres
  postgres-local-6:
    image: postgres:15-alpine
    container_name: postgres-local-6
    environment:
      POSTGRES_DB: fuzzilli_local
      POSTGRES_USER: fuzzilli
      POSTGRES_PASSWORD: fuzzilli123
      POSTGRES_INITDB_ARGS: "--encoding=UTF-8 --lc-collate=C --lc-ctype=C"
    volumes:
      - postgres_local_data_6:/var/lib/postgresql/data
      - /home/tropic/vrig/fuzzilli-vrig-proj/fuzzillai/postgres-init.sql:/docker-entrypoint-initdb.d/init.sql
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U fuzzilli -d fuzzilli_local"]
      interval: 10s
      timeout: 5s
      retries: 5
    restart: unless-stopped
    networks:
      - fuzzing-network

  # Worker 6 - Fuzzilli Container
  fuzzer-worker-6:
    build:
      context: /home/tropic/vrig/fuzzilli-vrig-proj/fuzzillai
      dockerfile: Cloud/VRIG/Dockerfile.distributed
    container_name: fuzzer-worker-6
    environment:
      - MASTER_POSTGRES_URL=postgresql://fuzzilli:fuzzilli123@postgres-master:5432/fuzzilli_master
      - LOCAL_POSTGRES_URL=postgresql://fuzzilli:fuzzilli123@postgres-local-6:5432/fuzzilli_local
      - POSTGRES_URL=postgresql://fuzzilli:fuzzilli123@postgres-local-6:5432/fuzzilli_local
      - FUZZER_INSTANCE_NAME=fuzzer-6
      - SYNC_INTERVAL=300
      - TIMEOUT=2500
      - MIN_MUTATIONS_PER_SAMPLE=25
      - DEBUG_LOGGING=false
    depends_on:
      postgres-master:
        condition: service_healthy
      postgres-local-6:
        condition: service_healthy
    volumes:
      - fuzzer_data_6:/home/app/Corpus
      - /home/tropic/vrig/fuzzilli-vrig-proj/fuzzbuild:/home/app/fuzzbuild:ro
    restart: unless-stopped
    healthcheck:
      test: ["CMD-SHELL", "pgrep -f FuzzilliCli || exit 1"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 60s
    networks:
      - fuzzing-network
    deploy:
      resources:
        limits:
          memory: 2G
        reservations:
          memory: 1G


  # Worker 7 - Local Postgres
  postgres-local-7:
    image: postgres:15-alpine
    container_name: postgres-local-7
    environment:
      POSTGRES_DB: fuzzilli_local
      POSTGRES_USER: fuzzilli
      POSTGRES_PASSWORD: fuzzilli123
      POSTGRES_INITDB_ARGS: "--encoding=UTF-8 --lc-collate=C --lc-ctype=C"
    volumes:
      - postgres_local_data_7:/var/lib/postgresql/data
      - /home/tropic/vrig/fuzzilli-vrig-proj/fuzzillai/postgres-init.sql:/docker-entrypoint-initdb.d/init.sql
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U fuzzilli -d fuzzilli_local"]
      interval: 10s
      timeout: 5s
      retries: 5
    restart: unless-stopped
    networks:
      - fuzzing-network

  # Worker 7 - Fuzzilli Container
  fuzzer-worker-7:
    build:
      context: /home/tropic/vrig/fuzzilli-vrig-proj/fuzzillai
      dockerfile: Cloud/VRIG/Dockerfile.distributed
    container_name: fuzzer-worker-7
    environment:
      - MASTER_POSTGRES_URL=postgresql://fuzzilli:fuzzilli123@postgres-master:5432/fuzzilli_master
      - LOCAL_POSTGRES_URL=postgresql://fuzzilli:fuzzilli123@postgres-local-7:5432/fuzzilli_local
      - POSTGRES_URL=postgresql://fuzzilli:fuzzilli123@postgres-local-7:5432/fuzzilli_local
      - FUZZER_INSTANCE_NAME=fuzzer-7
      - SYNC_INTERVAL=300
      - TIMEOUT=2500
      - MIN_MUTATIONS_PER_SAMPLE=25
      - DEBUG_LOGGING=false
    depends_on:
      postgres-master:
        condition: service_healthy
      postgres-local-7:
        condition: service_healthy
    volumes:
      - fuzzer_data_7:/home/app/Corpus
      - /home/tropic/vrig/fuzzilli-vrig-proj/fuzzbuild:/home/app/fuzzbuild:ro
    restart: unless-stopped
    healthcheck:
      test: ["CMD-SHELL", "pgrep -f FuzzilliCli || exit 1"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 60s
    networks:
      - fuzzing-network
    deploy:
      resources:
        limits:
          memory: 2G
        reservations:
          memory: 1G


volumes:
  postgres_local_data_1:
  fuzzer_data_1:
  postgres_local_data_2:
  fuzzer_data_2:
  postgres_local_data_3:
  fuzzer_data_3:
  postgres_local_data_4:
  fuzzer_data_4:
  postgres_local_data_5:
  fuzzer_data_5:
  postgres_local_data_6:
  fuzzer_data_6:
  postgres_local_data_7:
  fuzzer_data_7:
